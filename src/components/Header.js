import React from 'react'

function Header() {
  return (

          <span onClick={() => window.scroll(0,0)} className='header'>
              BLOCKBUSTER
          </span>
          

  )
}

export default Header